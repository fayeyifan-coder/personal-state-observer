# 个人状态观察器 V1.3

本版本在 V1.0 的月度观察基础上加入数据主权：完整 JSON 备份、导入预览与本机恢复。

## 新增
- 用户可以从月视图或年视图进入「标记一段时期」模式。
- 在时间轴上先点开始日期，再点结束日期；跨月选择可以继续用月导航完成。
- 创建标记时只保存：名称、开始日期、结束日期、创建/更新时间、`kind=user_period`。
- 同一段时间允许存在多个不同标记，不做互斥。
- 月视图显示时期横条；年视图显示按月份压缩的时期横条。
- 点击已有标记可以查看日期范围并删除。

## 语义原则
时期标记是用户自己的解释，不是系统生成的诊断、性格或因果结论。
系统不会根据心情、睡眠或身体数据自动生成「冬眠期」等标签。

## 数据
时期标记使用独立 IndexedDB object store `period_labels`，不会写进 `daily_records`。

## 运行
直接打开 `index.html`，或使用目录中的 `start-local.bat` 启动本地服务器。


## V1.1 数据主权
- 新增“数据”页面，显示本机记录概况。
- 一键导出完整 JSON 备份，包含每日记录、时期标记、版本信息与导出时间。
- 导入前先校验并预览记录条数、时期标记数量与日期范围。
- 恢复采用“替换本机数据”，确认后才执行。
- 不使用账号、服务器或云端同步。


## V1.3 手机端视觉整理
- 首页与记录页进一步压缩纵向空白，使手机端更像连续的观察记录，而不是传统表单。
- 主按钮改为炭黑，橄榄绿只承担选择状态与焦点提示，避免健康管理类产品的视觉暗示。
- 去掉底部导航的毛玻璃效果，改为实体纸面层。
- 统一页面底部安全区，避免底部导航遮挡内容。
- 增加键盘焦点样式和输入框聚焦反馈。
- 新增“私人观察台”轻量页头识别，保持档案 / 观察笔记气质。
- 卡片阴影进一步弱化，减少 Dashboard 感。


## V1.4｜记录行为语义
- 新建记录与修改已完成记录分开处理。
- 修改已完成记录时，中途离开不会把它改成“未完成”；已经改动的内容即时保存。
- 退出文案改为“这次记录先到这里？” / “修改先到这里？”，避免把记录过程描述成表单提交。
- 单选题不再使用“跳过”，统一使用“暂不记录”；多选题有选择时显示“下一项”。
- 从时间轴编辑某一天后，离开或完成修改都会回到当天详情，而不是跳回今天首页。
- “返回”只负责回到上一道当前可见的问题，不改变已保存的数据。
