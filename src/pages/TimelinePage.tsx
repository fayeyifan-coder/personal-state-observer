import { BottomNav } from '../components/BottomNav'
export function TimelinePage() { return <div className="page shell"><header className="brand">时间轴</header><section className="empty-state"><p>时间轴基础已迁移。</p><p>下一阶段把 V1.4 的月 / 年观察层完整迁入这里。</p></section><BottomNav current="/timeline" /></div> }
