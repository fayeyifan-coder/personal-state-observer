# 个人状态观察器｜Gemini 工程协作说明

## 你现在在做什么

这是一个“个人状态观察器（Personal State Observer）”的私人、local-first PWA。
它不是健康评分、医疗诊断、打卡、习惯管理或周期预测工具。
核心问题是：**今天的你是什么样子？**

用户每天通过压缩式问答留下少量真实状态数据；前面的回答会决定后面是否值得继续问。
系统应该尽量少问，而不是追求填写完整。

## 已经完成并经过真实使用验收的版本

`reference-v1.4-prototype/` 是 V1.4 原型。
它已经由用户实际操作测试，当前产品行为基本冻结。

V1.4 的关键特性：
- “去记录 / 今天不记录”双入口
- 一题一屏、自动保存
- 条件式问答
- 母节点 / 子节点结构
- 睡眠很好时不展开无意义的睡眠细节
- 只有出现相关睡眠问题时才追问睡前尿意、夜间饥饿等
- 支持中途退出、恢复、编辑过去记录
- 已完成记录的编辑不会错误地变成未完成记录
- 月 / 年时间轴
- 时期标记
- JSON 导入 / 导出
- local-first，不上传个人记录

## 非常重要的产品原则

### 1. 数据先记录，意义以后产生
不要自动生成：
- 健康分数
- 情绪分数汇总
- 诊断
- 因果结论
- “你进入了某某状态”的系统判断

未来可以做事实统计，但只陈述观察到的关系。

### 2. 没问 ≠ 没发生
隐藏的子问题字段应该保持 `null`，不能自动写成 negative / none。

### 3. 不追求每天问题数量相同
普通的一天应该非常短；真正有异常的一天才自然展开。

### 4. “今天不记录”是真实行为
- 有记录：`●`
- 主动不记录：`○`
- 草稿：`◐`
- 没打开：空白

不要设计 streak、完成率、连续打卡。

### 5. 用户解释拥有最终权力
时期标记由用户命名，例如“冬眠期”“搬家那段时间”。
系统不能根据数据自动替用户命名。

### 6. 产品语言
聊天和产品设计文档中使用：
- 母节点
- 子节点
- 条件展开
- 问题路径

不要擅自改成“父节点/子节点”。

## 当前工程

技术栈：
- React
- TypeScript
- Vite
- Dexie
- vite-plugin-pwa

目录：
- `src/data/questions.ts`：问题配置
- `src/logic/`：条件判断、问题引擎、失效回答清理
- `src/db/`：Dexie / IndexedDB
- `src/pages/`：Today / Record / Timeline / Data
- `src/components/`：可复用 UI
- `reference-v1.4-prototype/`：行为基准，不要直接继续堆新功能

## 当前目标：V1.5

只做“工程化迁移”，不要增加产品功能。

目标：
1. 完整迁移 V1.4 Today / Record 行为
2. 完整迁移月 / 年时间轴
3. 完整迁移时期标记
4. 完整迁移 JSON 导入 / 导出
5. 保证 IndexedDB 数据结构稳定
6. 添加自动化测试
7. 确认 `npm run build` 正常
8. 保持视觉与行为与 V1.4 一致

## 第一任务（现在只做这一件）

请先不要大规模改代码。

先完整阅读：
- `README.md`
- `MIGRATION.md`
- `src/data/questions.ts`
- `src/logic/conditionEvaluator.ts`
- `src/logic/questionEngine.ts`
- `src/db/database.ts`
- `src/db/records.ts`
- `reference-v1.4-prototype/questions.js`
- `reference-v1.4-prototype/question-engine.js`
- `reference-v1.4-prototype/app.js`

然后完成一次“迁移审查”：

1. 找出 V1.5 与 V1.4 行为基准之间尚未实现的部分。
2. 按“必须先迁移 / 可以后迁移”分类。
3. 检查 TypeScript 类型是否能够覆盖当前问题树。
4. 检查母节点 / 子节点条件是否存在逻辑漏洞。
5. 检查 IndexedDB 模型是否支持：draft / recorded / opted_out、编辑、过去日期、null 语义。
6. 检查是否存在会破坏“数据先记录、意义以后产生”原则的设计。
7. 不要修改业务逻辑，不要新增问题，不要加入 AI 分析。

最后只输出：
- 审查结果
- 发现的问题
- 推荐的迁移顺序
- 每一步预计修改哪些文件

不要直接重构整个项目。

## 后续协作规则

当开始真正写代码后：
- 每次只完成一个可验证的小任务
- 修改前说明要保持哪些 V1.4 行为不变
- 修改后运行测试
- 不要为了“代码更漂亮”而改变产品行为
- 不要引入额外库，除非说明必要性
- 所有个人数据功能默认 local-first
- 不要把用户记录发送到服务器
